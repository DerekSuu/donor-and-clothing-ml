# Outputs

This directory is used for output files generated by the notebook.

The main output is:

```text
outputs/clustering_results.csv
```

It contains one row per image:

- `Filename`
- `Cluster`
- `Cluster_Assessment`

The file is created after running the image clustering workflow. Its contents may vary depending on the selected final cluster count and the manual cluster-level assessments.
